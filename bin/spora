#!/usr/bin/env php
<?php

declare(strict_types=1);

// Entry point: bin/ → consumer root via dirname 2 levels up.
// The framework's bin/spora (required below) loads vendor/autoload.php.
define('BASE_PATH', dirname(__FILE__, 2));

require BASE_PATH . '/vendor/spora-ai/spora-core/bin/spora';


